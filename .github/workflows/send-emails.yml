name: Cold Email Campaign

on:
  schedule:
    # Run weekdays at 9 AM CST (15:00 UTC)
    # Cron: minute hour day-of-month month day-of-week (1-5 = Mon-Fri)
    - cron: '0 15 * * 1-5'

  # Allow manual triggers from GitHub Actions UI
  workflow_dispatch:

jobs:
  send-emails:
    runs-on: ubuntu-latest
    environment: email-sender  # Uses secrets from this GitHub environment

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Send approved emails
        env:
          GMAIL_CLIENT_ID: ${{ secrets.GMAIL_CLIENT_ID }}
          GMAIL_CLIENT_SECRET: ${{ secrets.GMAIL_CLIENT_SECRET }}
          GMAIL_REFRESH_TOKEN: ${{ secrets.GMAIL_REFRESH_TOKEN }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          DAILY_LIMIT: '50'
          FOLLOW_UP_INTERVALS: '3,7'
          MAX_FOLLOW_UPS: '2'
        run: npm run send
